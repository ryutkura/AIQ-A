---
type: system
created: 2026-06-15
updated: 2026-07-06
---
# AI Agent Instructions for AIQandA Workspace

このディレクトリで作業するAIエージェント（あなた）は、ユーザーとの対話において以下のルールを厳守してください。このファイルはシステムプロンプトの一部として機能します。

## 1. ディレクトリの目的と基本ルール
* これはAIに質問した内容をmarkdownファイルに書き留めるディレクトリです。
* QandAの内容を、質問の文脈に応じた**適切なフォルダの適切なファイル**に書き綴ってください。
* QandAの内容が、前述の質問の続きの場合や続けた方がmarkdownファイルが一つの記事としてまとまる場合は**先ほどまで書き込んでいたファイル**に続ける形で書き綴ってください。   
この場合既に既存のファイルに似たような内容のファイルがあった場合はそのファイルに続きを書いても大丈夫です。
* もし適切なファイルやフォルダが存在しない場合は、**あなた自身が自律的に新規作成**してそこに記述してください。

### 1-1. 新規ノートには必ず frontmatter を付けること

ノートを新規作成する際は、必ず先頭に以下を書いてください。これが無いとObsidianの絞り込み・ダッシュボードが機能しません。

```
---
type: qa
tags: [フォルダに対応するタグ]
created: YYYY-MM-DD
updated: YYYY-MM-DD
status: seed
---
```

`type` は `qa`（通常の質問）/ `troubleshoot`（ハンズオンの沼ログ）/ `concept`（概念ノート）から選びます。

### 1-2. どのフォルダに書くか

| 内容 | 置き場所 |
| --- | --- |
| 通常の質問と解説 | トピック別フォルダ（`math`, `programming` など） |
| **ハンズオンで詰まった話・エラー** | `troubleshooting/<領域>/` |
| **教科書・長文の学習素材** | `textbooks/<本のタイトル>/`（章ごとに1ファイル） |
| 分類に迷う走り書き | `inbox/`（後で振り分ける） |
| Qiita/Zenn 投稿用の下書き | `publish/drafts/`（**手で書かない**。後述のツールで書き出す） |

### 1-2-1. 教科書について読んだ質問をどこに書くか

**教科書のフォルダの中に質問を書いてはいけません。** 質問の中身は「その分野の知識」なので、
通常どおり**トピック別フォルダ**に書き、本文から**章へのリンクを張ってください。**

```markdown
---
type: qa
tags: [math, linear-algebra]
---
# 特異値分解の幾何的な意味

[[textbooks/線形代数入門/05_特異値分解]] の p.3 で詰まった。
```

こうする理由:

* 質問の内容は `concepts/` と繋がるべき知識であり、教科書の章は「文脈」に過ぎない
* 「この章についての質問を一覧したい」は、Obsidianの**バックリンクが自動で解決する**ため、
  物理的に同じフォルダへ置く必要がない

`textbooks/` 配下は `status: unread` が既定です。読み終えた章は `status: read` に変えると、
`moc/ホーム` の「未読の章」から消えます。

### 1-3. 触ってはいけない場所

* `concepts/` — `tools/make_concepts.py` の生成物。定義を直したい時は `tools/concept_defs.md` を編集する。
* `moc/` — `tools/make_moc.py` の生成物。手で書き足しても再生成で消える。
* `publish/` — `tools/export_for_publish.py` の出力先。正本は Vault 側のノート。

## 2. ハンズオンの「沼」を記録する（最重要）

ユーザーがハンズオンやセットアップで詰まっている時、その対話は**そのまま沼ログにする価値があります。**
以下を厳守してください。

### 2-1. 沼ログを作るタイミング

* ユーザーがエラーメッセージを貼ってきた
* 「動かない」「なぜかできない」と言っている
* 同じ問題で2往復以上やりとりした

このいずれかに当てはまったら、**解決した直後に**`troubleshooting/<領域>/` へノートを作ってください。
ユーザーに「記録しますか？」と尋ねる必要はありません。作ってから報告してください。

### 2-2. ファイル名

沼は概念ではなく**イベント**なので、日付を前に置きます。

```
troubleshooting/docker/2026-08-09_port-already-allocated.md
```

### 2-3. 必ず書くこと

雛形は `templates/沼ログ.md` にあります。frontmatter には以下を入れてください。

```
---
type: troubleshoot
tags: [docker]
error: "実際のエラーメッセージの特徴的な部分"
env: "Windows 11 / Docker Desktop 27.x"
created: YYYY-MM-DD
updated: YYYY-MM-DD
status: solved      # solved | workaround | unsolved
stuck_time: 90min
---
```

本文で**絶対に省略してはいけない**のは次の3つです。

1. **エラーの全文（テキストで）**
   要約してはいけません。後日ユーザーが同じエラーに遭遇した時、
   **エラー文をそのまま全文検索して過去の自分に辿り着く**のが、このノートの最大の使い道だからです。

   スクリーンショットを**添える**のは歓迎ですが、**代用してはいけません。**
   画像は全文検索にかからないため、テキストが無いとこのノートの最大の使い道が失われます（5-1 を参照）。

2. **沼ログ（試したことの表）**
   「何を試したか」だけでなく **「なぜそう考えたか（仮説）」と「なぜダメだったか」** を書きます。
   これが無いと、次回また同じ袋小路に入ります。対話の履歴から復元して埋めてください。

3. **「なぜ沼ったか」**
   このノートで最も価値がある欄です。「知らなかった」で終わらせず、
   **どんな思い込みが判断を誤らせたか**を1〜2文で言語化します。
   （例：「`docker start` がコンテナを作り直すと思い込んでいた」
   　　　「エラーの1行目だけ読み、本当の原因が書かれた末尾を読んでいなかった」）

### 2-4. 解決しなかった場合

`status: unsolved` にして、**そこまでの沼ログを残してください。** 消してはいけません。
「試したがダメだった手」の記録は、次に再挑戦する時に最も価値があります。

---

## 3. 別リポジトリからのノート移植

勉強用・開発用の別リポジトリで溜めた技術ノートを持ち込む場合の手順。

ユーザーが「別のところで勉強したい」「あっちで質問を溜めたい」と言った場合は、
`templates/外部リポジトリ用_指示書.md` を**そのリポジトリのルートに `CLAUDE.md` という
名前でコピーする**よう案内してください（`AGENTS.md` が要る場合は、全文コピーではなく
`CLAUDE.md` を指す1行だけのファイルにすること）。
このリポジトリの `CLAUDE.md` はポインタですが、**向こうの `CLAUDE.md` は中身そのもの**です。
役割が違うので混同しないでください。
この指示書には、移植先（このリポジトリ）と揃った frontmatter 形式、
ノイズとして省くべき情報、書き終わりの条件が書いてあります。

移植する時:

1. 向こうの `technote/<分野>/` を、こちらの同名フォルダへコピーする
2. **ソースコード（`.py` など）は移植しない。** Markdown だけを持ち込む
3. 通常のパイプライン（第6節）を順に実行する

**移植してよいか一度立ち止まること。** 使い捨ての詰まり（タイポ、インデントミス）まで
持ち込むとナレッジが薄まります。判断基準は「半年後に読む価値があるか」です。
明らかに使い捨てのものが混ざっていたら、ユーザーに指摘してください。

## 4. Qiita / Zenn への投稿

ユーザーが「記事にしたい」「投稿用にまとめて」と言った場合：

```bash
python tools/export_for_publish.py <ノートのパス> --target zenn --write
python tools/export_for_publish.py <ノートのパス> --target qiita --write
```

**手で `publish/` にMarkdownを書かないこと。** 必ずこのツールを通してください。理由：

* QiitaもZennもObsidianのリンク記法（二重角括弧）を**レンダリングしません**。本文に記号がむき出しで出ます。
* frontmatter の仕様が両者でまったく違います。
* **Qiitaはローカル画像を参照できません。** 画像がある記事は、ユーザーにQiitaへのアップロードとURL差し替えが必要だと必ず伝えてください。

正本はVault側のノートです。`publish/` は書き出し先であり、内容を育てる場所ではありません。

## 5. クリップボード画像の読み込み機能（重要）
ユーザーが「画像を見て」「クリップボードを確認して」「このスクショを解説して」などの指示を出した場合、ユーザーにいちいちやり方を尋ねる事なく、直ちに以下の手順を実行してください：

1. 端末で `python tools/get_clipboard.py` を実行する。
2. 成功するとカレントディレクトリに `_temp_clipboard.png` が生成されるため、その画像ファイルを読み込み、ユーザーの質問に答える。
3. 解説を作成する際、**その画像を残すべきかを 5-1 の基準で判断すること。** 残す場合のみ `_temp_clipboard.png` を `image/` フォルダ配下に適切な名前をつけてコピー保存し、Markdownファイルの適切な位置（沼ログなら「症状」の節）にその画像リンク（`![画像名](../image/ファイル名.png)` など）を記述する。
4. 画像の解析および保存作業が完了したら（あるいはエラーが起きた場合も）、必ず端末で `del _temp_clipboard.png` を実行し、ルートディレクトリの**一時ファイルを完全に削除（クリーンアップ）する**こと。   

### 5-1. 画像を残すかどうかの判断基準

このリポジトリのMarkdownは、**後から自分専用の参考書として読み返す**目的も持っている。
読み返した時に当時の状況が蘇る画像には価値があるので、**迷ったら残す方に倒してよい。**
容量は制約にならない（1枚あたり数百KB、この規模なら数年分溜めても問題にならない）。

ただし次の2つだけは必ず守ること。

**(1) エラー・ログ・コマンドは、画像を残す場合でも必ずテキストでも貼ること。**

画像は全文検索にかからない。後日同じエラーに遭遇した時、
**エラー文をそのまま検索して過去の自分に辿り着く**のが沼ログの最大の使い道なので、
テキストが無いとその用途が丸ごと失われる。
スクリーンショットは「テキストの代わり」ではなく「テキストに添えるもの」。

**(2) 本文から参照しない画像は `image/` に保存しないこと。**

どのノートからも参照されない画像は、後から用途が分からなくなり、参考書としても機能しない。
残すなら必ず本文から参照する。

**特に残す価値が高いもの**

* 数式、図、グラフ、回路図など、文字に起こすと失われる情報
* GUIの画面状態（設定画面、ダイアログ、メニューの位置）
* レイアウト崩れ・描画バグなど、見た目そのものが問題である場合
* エラー画面全体（周辺の出力やターミナルの状態など、エラー文だけでは失われる文脈がある場合）


## 6. ナレッジグラフの更新

ユーザーが「キーワードをリンク化して」「グラフを更新して」と求めた場合や、ノートを複数追加した後は、以下を**この順番で**実行してください。

全てのツールは**既定では書き込みません**。必ず「プレビュー → 件数を提示 → 了承を得て適用」の順で進めること。

| # | コマンド | 用途 |
| --- | --- | --- |
| 0 | `python tools/link_keywords.py` | 現状診断（リンク解決率・破損箇所・ハブ化した語） |
| 1 | `python tools/link_keywords.py --link --write` | キーワードをリンク化（1ファイル1語につき初出のみ） |
| 2 | `python tools/make_concepts.py --write` | 2記事以上から参照された語を `concepts/` に追加 |
| 3 | `python tools/add_frontmatter.py --write` | frontmatter の無いノートに付与 |
| 4 | `python tools/make_moc.py --write` | `moc/` を再生成 |

その他:

| コマンド | 用途 |
| --- | --- |
| `python tools/link_keywords.py --repair --write` | コード内リンクや破損リンクの修復 |
| `python tools/link_keywords.py --prune --write` | キーワード定義から外した語のリンクを解除 |
| `python tools/link_keywords.py --headings --write` | 見出し行のリンクを解除 |

**厳守事項:**

* リンク対象語は `tools/keywords.txt`、概念の定義は `tools/concept_defs.md` にある。**スクリプト本体を編集しないこと。**
* `--write` を伴うコマンドの実行前に、必ずプレビューを実行して変更件数をユーザーに提示し、了承を得ること。
* 全ツールは冪等（何度実行しても結果が変わらない）に作られている。**実行するたびに件数が増える場合はバグなので、適用せずユーザーに報告すること。**
* リンクツールはコードブロック・インラインコード・frontmatter・見出し・既存リンクを自動的に保護する。**コードブロックやインラインコードの中にリンク記法（二重角括弧）を手で書き加えてはならない**（コードが壊れ、コピペしても動かなくなるため）。
* 汎用語（`設計` `クラス` `変数` `関数` など）をキーワードに追加しないこと。全ノートに繋がるハブになり、グラフの情報量がゼロになる。そうした語は frontmatter の `tags:` に書く。
